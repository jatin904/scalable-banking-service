apiVersion: v1
kind: ConfigMap
metadata:
  name: notification-config
data:
  # Spring environment/profile
  SPRING_PROFILES_ACTIVE: "smtp"
  SERVER_PORT: "8080"

  # Mail settings (non-secret part)
  MAIL_HOST: "smtp.gmail.com"
  MAIL_PORT: "587"
  MAIL_FROM: "noreply2024tm93003@gmail.com"

  # Legacy 2Factor settings (if you still use 2Factor.in)
  NOTIFICATION_SMS_BASE_URL: "https://2factor.in/API/V1"
  NOTIFICATION_SMS_SENDER_ID: "TFCTOR"
  NOTIFICATION_SMS_MOCK_DELIVERY: "false"

  # Application-specific defaults
  NOTIFICATION_MAIL_MOCK_DELIVERY: "false"
  NOTIFICATION_THRESHOLD_HIGH_VALUE_TRANSACTION: "10000"
