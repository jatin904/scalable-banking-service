apiVersion: v1
kind: Secret
metadata:
  name: notification-secrets
type: Opaque
stringData:
  # SMTP credentials
  MAIL_USERNAME: "noreply2024tm93003@gmail.com"
  MAIL_PASSWORD: "qvhm nvlx ajvw ohvm"

  # 2Factor API key (legacy)
  NOTIFICATION_SMS_API_KEY: "dc474765-bbeb-11f0-bdde-0200cd936042"

  # Twilio credentials (used when provided)
  TWILIO_ACCOUNT_SID: ""
  TWILIO_AUTH_TOKEN: ""
  TWILIO_FROM_NUMBER: "+18782511081"

  # Optional DB password (if using the bundled Postgres below)
  POSTGRES_PASSWORD: "changeme"
  POSTGRES_USER: "notification"
  POSTGRES_DB: "notificationdb"

# Note: stringData allows you to define plaintext values here; in production prefer creating
# the Secret with kubectl create secret generic --from-literal=... or a secrets manager integration.
